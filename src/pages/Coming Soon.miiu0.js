// Coming Soon page script placeholder (syntax-safe)
$w.onReady(function () {
  // Intentionally minimal: OTP utilities were moved/disabled to prevent build errors.
  // If OTP is required on this page, expose backend functions in a .jsw module and call them from here.
});

