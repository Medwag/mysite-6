// backend/qr-service.jsw
import { fetch } from 'wix-fetch';

export async function generateQRCode(url) {
  const apiUrl = `https://api.qrserver.com/v1/create-qr-code/?size=250x250&data=${encodeURIComponent(url)}`;
  return apiUrl;
}
